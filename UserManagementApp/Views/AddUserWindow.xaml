<Window x:Class="UserManagementApp.Views.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserManagementApp"
        xmlns:converters="clr-namespace:UserManagementApp.Converters"
        mc:Ignorable="d"
        Title="Добавить пользователя" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Имя пользователя -->
        <Label Grid.Row="0" Grid.Column="0" Content="Имя пользователя:" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 Height="25" Margin="0,0,0,10"/>

        <!-- Email -->
        <Label Grid.Row="1" Grid.Column="0" Content="Email:" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                 Height="25" Margin="0,0,0,10"/>

        <!-- Роль -->
        <Label Grid.Row="2" Grid.Column="0" Content="Роль:" 
               VerticalAlignment="Center" Margin="0,0,10,10"/>
        <ComboBox Grid.Row="2" Grid.Column="1" 
                  ItemsSource="{Binding Roles}"
                  SelectedItem="{Binding SelectedRole}"
                  DisplayMemberPath="Name"
                  Height="25" Margin="0,0,0,10"/>

        <!-- Сообщение об ошибке -->
        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="{Binding ValidationMessage}"
                   Foreground="Red" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Visibility="{Binding ValidationMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Индикатор загрузки -->
        <ProgressBar Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                     Height="5" 
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Margin="0,0,0,10"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" 
                    Width="80" Height="30" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" 
                    Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
